# Dependencies
node_modules/

# npm local cache used when the global cache is outside the sandbox
.npm-cache/

# Lockfile is committed: CI and publish workflows run `npm ci` for
# reproducible installs (dev tooling only — runtime stays dependency-free).

# Vendored third-party reference material (engine source/tests), kept local
refs/

# Local scratch / research drafts
scratch/
prime-agent-refine-research.md
dsh-continual-harness-evaluation.md

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Build output
dist/
coverage/
*.tgz

# Internal verification notes
VERIFY.md
